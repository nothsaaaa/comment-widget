<!DOCTYPE html>
<html>
<head>
  <title>Comment Widget Generator</title>
</head>
<body>
  <h1>Create Your Comment Section</h1>
  <form action="/generate" method="post">
    <button type="submit">Generate Comment Widget</button>
  </form>

  <% if (embedCode) { %>
    <h2>Your Embed Code:</h2>
    <textarea rows="4" cols="100" readonly><%= embedCode %></textarea>

    <h3>Preview:</h3>
    <div>
      <%- embedCode %>
    </div>

    <h3>Admin Details</h3>
    <p><strong>Password:</strong> <code><%= adminPassword %></code></p>
    <p><strong>Admin Panel:</strong> 
      <a href="/admin/<%= embedCode.match(/widget\/(.+?)"/)[1] %>" target="_blank">
        Go to Admin Panel
      </a>
    </p>
  <% } %>
</body>
</html>
