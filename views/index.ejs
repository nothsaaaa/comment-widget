<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Comment Widget Generator</title>
  <style>
    body {
      font-family: sans-serif;
      font-size: 14px;
      margin: 10px;
      background-color: #1e1e2f;
      color: #eee;
    }
    .container {
      max-width: 800px;
      margin: auto;
      padding: 20px;
    }
    form {
      margin-bottom: 15px;
      text-align: center;
    }
    textarea {
      width: 100%;
      height: 70px;
      padding: 8px;
      margin-top: 5px;
      box-sizing: border-box;
      border: 1px solid #444;
      border-radius: 6px;
      background-color: #2c2c3e;
      color: #eee;
      resize: vertical;
      font-family: monospace;
      font-size: 14px;
    }
    button {
      margin-top: 6px;
      padding: 8px 12px;
      background-color: #5e2ca5;
      color: white;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
      transition: background-color 0.3s ease;
    }
    button:hover {
      background-color: #4b2386;
    }
    h1, h2, h3 {
      color: #eee;
      margin-top: 0;
    }
    .preview {
      padding: 10px;
      background-color: #292940;
      border-radius: 6px;
      border: 1px solid #444;
      margin-bottom: 20px;
      min-height: 120px;
    }
    .admin-details p {
      margin: 5px 0;
    }
    .admin-details code {
      background-color: #2c2c3e;
      padding: 2px 6px;
      border-radius: 4px;
      font-weight: 600;
      color: #cdaeff;
    }
    .admin-details a {
      color: #cdaeff;
      text-decoration: none;
    }
    .admin-details a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 style="text-align: center;">Create Your Comment Section</h1>
    <form action="/generate" method="post">
      <button type="submit">Generate Comment Widget</button>
    </form>

    <% if (embedCode) { %>
      <h2 style="margin-bottom: 10px;">Your Embed Code:</h2>
      <textarea rows="6" readonly><%= embedCode %></textarea>

      <h3 style="margin-top: 10px;">Preview:</h3>
      <div class="preview">
        <%- embedCode %>
      </div>

      <div class="admin-details">
        <h3>Admin Details</h3>
        <p>Password: <code><%= adminPassword %></code></p>
        <p>Admin Panel:
          <a href="/admin/<%= embedCode.match(/widget\/(.+?)"/)[1] %>" target="_blank" rel="noopener noreferrer">
            Go to Admin Panel
          </a>
        </p>
      </div>
    <% } %>
  </div>
</body>
</html>
